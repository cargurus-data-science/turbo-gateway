[project]
name = "turbo-gateway-authorizer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3",
]

[tool.github-actions.options]
deploy_dev_image = true
deploy_prod_image = true
